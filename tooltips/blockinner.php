<?php ?>
<div id="docs-blockinner" class="docs-wrap">
<h3><?= @text('Block Inner Explained') ?>:</h3>
<p class="intro"><?= @text('Similar to the Block Wrap, enabling block inner will give you an additional div nested inside the block. This is handy when you need to have a fixed width layout, but need to pad inwards without breaking the layout. Our aim with Morph is to ensure that no code is loaded for anything if it is not needed.') ?></p>
</div>